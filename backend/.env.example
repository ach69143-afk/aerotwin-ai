# Set these in the process environment or your hosting provider's secret store.
# Do not commit a real operator token.
AEROTWIN_ENV=production
AEROTWIN_ALLOWED_ORIGINS=https://your-dashboard.example.com
AEROTWIN_CONTROL_TOKEN=replace-with-a-long-random-operator-token
