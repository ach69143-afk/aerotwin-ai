# Public backend endpoint. Keep local development on localhost by omitting this.
VITE_API_URL=https://your-api.example.com

# Optional shared operator token for a protected, access-controlled dashboard.
# Do not use this in a public client; route controls through authenticated server-side code.
VITE_CONTROL_TOKEN=
